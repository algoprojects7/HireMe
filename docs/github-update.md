git add -A; git commit -m "Fixed: Navigation hash issue"; git push

Search & Booking Detail:

No searching module will works as follows : Provider will input a location at the top of the map eg. Jalukbari and skill, can select both or any --> At the map the workers or leaders will be listed with skills, rating, dp --> Provider selects a user --> View his skills, rating, and DP --> Call/Message ---> Redirect if not logged in --> if logged in --> Proceed for call/Message --> Booking communication --> Payment through Razorpay with platform charge and service charge for razorpay --> Booking confirmed --> in late stage, after work is over, Worker sends a fedback as "No issues" --> Provider acknowledged it as "No issues" --> Amount transferred to the Worker Wallet with deduction for platform charge --> Worker can make request for transfer the amount fro Wallet to Bank/UPI using razorpay with service charge deduction

Payment gateway will be used from Algoguido technologies Private Limited as it is the parent company. The Account details for Razorpay is +91 8638526521
